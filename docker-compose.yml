services:
  mssql:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: mssql-lab9
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: ${SA_PASSWORD}
    volumes:
      - mssql-data:/var/opt/mssql
    networks:
      - is-net
    restart: unless-stopped

  app:
    image: isapp:lab8
    container_name: isapp-lab9
    environment:
      ASPNETCORE_URLS: http://0.0.0.0:8080
      ASPNETCORE_ENVIRONMENT: Production
      App__Name: IsLabApp
      App__Version: ${APP_VERSION}
      ConnectionStrings__Mssql: Server=mssql-lab9,1433;Database=IsLabDb;User Id=sa;Password=${SA_PASSWORD};TrustServerCertificate=True;
    ports:
      - "127.0.0.1:5001:8080"
    networks:
      - is-net
    depends_on:
      - mssql
    restart: unless-stopped

networks:
  is-net:
    driver: bridge

volumes:
  mssql-data:
